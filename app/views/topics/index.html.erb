<p id="notice"><%= notice %></p>

<h1>Topics</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Course</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= topic.name %></td>
        <td><%= topic.course %></td>
        <td><%= link_to 'Show', course_topic_path(@course, topic) %></td>
        <td><%= link_to 'Edit', edit_course_topic_path(@course, topic) %></td>
        <td><%= link_to 'Destroy', course_topic_path(@course, topic), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Topic', new_course_topic_path %>
